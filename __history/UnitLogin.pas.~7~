unit UnitLogin;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants,
  FMX.Types, FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Dialogs,
  FMX.StdCtrls, FMX.Controls.Presentation, FMX.Edit, FMX.Objects,
  FMX.Layouts,
  UnitRegister,      // forma za registraciju
  UnitMain;          // glavna forma posle uspešnog logina

type
  TfrmLogin = class(TForm)
    recOverlay: TRectangle;
    lblTitle: TLabel;
    lblSubtitle: TLabel;
    edtEmail: TEdit;
    edtPassword: TEdit;
    btnShowPassword: TSpeedButton;
    btnLogin: TButton;
    btnNoAccount: TButton;           // novo dugme za prelazak na registraciju
    procedure FormCreate(Sender: TObject);
    procedure btnShowPasswordClick(Sender: TObject);
    procedure btnLoginClick(Sender: TObject);
    procedure btnNoAccountClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  frmLogin: TfrmLogin;

implementation

{$R *.fmx}

procedure TfrmLogin.FormCreate(Sender: TObject);
begin
  edtEmail.Text := '';
  edtPassword.Text := '';
  edtEmail.SetFocus;
end;

procedure TfrmLogin.btnShowPasswordClick(Sender: TObject);
begin
  edtPassword.Password := not edtPassword.Password;

  if edtPassword.Password then
    btnShowPassword.Text := '👁'          // sakriveno
  else
    btnShowPassword.Text := '👁‍🗨';       // vidljivo
end;

procedure TfrmLogin.btnLoginClick(Sender: TObject);
begin
  if edtEmail.Text.Trim.IsEmpty then
  begin
    ShowMessage('Unesite email adresu.');
    edtEmail.SetFocus;
    Exit;
  end;

  if edtPassword.Text.Trim.IsEmpty then
  begin
    ShowMessage('Unesite lozinku.');
    edtPassword.SetFocus;
    Exit;
  end;

  // privremena provera (kasnije zameni sa proverom iz baze)
  if (LowerCase(edtEmail.Text) = 'pera123@gmail.com') and
     (edtPassword.Text = '123456') then
  begin
    ShowMessage('Uspešno ste se prijavili!');

    edtEmail.Text := '';
    edtPassword.Text := '';

    if Assigned(frmMain) then
    begin
      frmMain.Show;
      Self.Hide;
    end
    else
    begin
      ShowMessage('Glavna forma (frmMain) nije kreirana!');
    end;
  end
  else
  begin
    ShowMessage('Pogrešan email ili lozinka.');
    edtPassword.Text := '';
    edtPassword.SetFocus;
  end;
end;

procedure TfrmLogin.btnNoAccountClick(Sender: TObject);
begin
  if Assigned(frmRegister) then
  begin
    frmRegister.Show;
    Self.Hide;
  end
  else
  begin
    ShowMessage('Forma za registraciju (frmRegister) nije kreirana!');
  end;
end;

end.
